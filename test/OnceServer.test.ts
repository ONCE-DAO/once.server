test("adds 1 + 2 to equal 3", () => {
  let one:number = 1
  let two:number = 2
  expect(one+two).toBe(3);
});

// test("import test", async () => {
//   const server = await OnceServer.start()
//   await server.start()
//   expect(server.runningPort).toBe(3000);
// });
